<%= simple_form_for(@cocktail, :html=> { class: 'bg-dark' }) do |f| %>
    <%= f.input :name, label: false  %>
    <%= f.button :submit, 'Update Name', class: 'btn btn-outline-secondary my-2'%>
<% end %>
